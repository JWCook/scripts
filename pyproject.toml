[tool.ruff]
fix = true
unsafe-fixes = true
line-length = 100
output-format = 'grouped'
select = ['B', 'C4', 'C90', 'E', 'F']
ignore = ['B023']
target-version = 'py310'

[tool.ruff.format]
quote-style = 'single'

[tool.ruff.lint.pycodestyle]
max-line-length = 120
